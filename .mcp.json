{
  "mcpServers": {
    "B12": {
      "command": "bash",
      "args": ["scripts/start-mcp.sh"],
      "env": {
        "MCP_EMBEDDING_MODEL": "BAAI/bge-m3",
        "MCP_MAX_RESPONSE_CHARS": "40000"
      }
    }
  }
}

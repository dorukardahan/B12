# B12 OpenCode Plugin

OpenCode integration for the B12 memory system. The package is distributed as a
JavaScript Bun plugin and keeps `better-sqlite3` external so native bindings are
resolved by the host install.

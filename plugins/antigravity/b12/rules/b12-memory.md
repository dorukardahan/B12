# B12 memory rules for Antigravity

- Use the B12 MCP tools when the user asks about prior decisions, lessons, project context, or cross-session memory.
- Treat injected B12 context as private working context. Do not reveal raw memory internals unless the user asks.
- Prefer concise citations to memory snippets over dumping long context.
- If B12 is unavailable, continue normally and mention the missing memory only when it affects the task.
